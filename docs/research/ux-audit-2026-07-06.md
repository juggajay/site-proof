# UX / Workflow Audit — 2026-07-06

Four parallel code-grounded audits (office quality loop, field shells,
cross-cutting, commercial chain), 48 findings, consolidated and ranked.
Batch 1 (data honesty) SHIPPED 2026-07-07: #1342 test register loads all
pages + ?test= deep link; #1343 ITP template propagate UI; #1344 offline
defect integrity (offline FAILs were 400-ing on sync and silently lost);
#1345 foreman-shell photo-required guard; #1346 trivial wins (error
toasts, shortcuts help, empty-state CTAs).

Batches 2–3 below are UNSHIPPED, awaiting owner go. File:line refs are as
of master ~`58017bfe`; re-verify before building.

## Batch 2 — daily-grind friction (approved shape, not yet built)

1. **Bulk docket approval** — office reviews one modal per docket;
   DocketApprovalsTable/MobileView per-row only, no selection state
   (DocketApprovalsPage.tsx:34-459). Fix: multi-select + "Approve
   selected (unadjusted)" skipping the modal when no hour changes.
2. **Crew entry "Save & add another"** — DocketScreen.tsx:316 closeSheet()
   after each add; resetSheetState() clears times/lot. Keep sheet open,
   retain times+lot.
3. **Conform CTA in the readiness panel/header** when canConform — button
   currently below all tabs (LotDetailPage.tsx:441 panel vs :509
   QualityManagementSection.tsx:189-199).
4. **Lot↔NCR context both directions** — no "Raise NCR" on lot NCRs tab
   (NCRsTabContent.tsx:46-51); rows not clickable / mobile goes to bare
   /ncr (:98, :121-143) though ?ncr= deep-link exists (NCRPage.tsx:63-71,
   ?create=1 at :74).
5. **Notification deep-links + internal NCR emails** — creation sites
   hardcode list URLs instead of buildProjectEntityLink (ncrCore.ts:341,
   ncrWorkflow.ts:222,299,497); internal users get NO email on NCR
   assign/status (ncrCore.ts:334 etc. lack sendNotificationIfEnabled;
   prefs exist and default ON, emailPreferences.ts:26).
6. **Photo failure honesty (shell)** — grid Retry badge inside
   pointer-events-none (PhotosListScreen.tsx:90,96); detail prose says
   "syncing on its own" on terminal error (PhotoDetailScreen.tsx:250 vs
   :277-281 — branch on syncState==='error').
7. **Diary entry edit/delete on phone** — WorkScreen rows' onEdit sets
   state nothing renders (useDiaryMobileHandlers.ts:335-338;
   WorkScreen.tsx:118,164). Navigate to /m/diary/work/:type pre-filled;
   add delete.
8. **One AUD formatter** — four formatters, field sees $1,235 vs office
   $1,234.56 (docketApprovalsData.ts:105 vs docketEditDisplay.ts:10-11,
   subcontractorDashboardHelpers.ts:22-23); Costs page + CSV round to
   whole dollars (costsPageHelpers.ts:60-67 used for display AND
   buildCostReportRows — keep cents in export).
9. **Sync status honesty (shell)** — SyncChip non-tappable
   (SyncChip.tsx:26-29), offline shows "0 waiting" (syncChipState.ts:17),
   retry only in lazy floating pill. Chip = single tap target, explicit
   Offline state.
10. **Draft dockets with hours invisible to office** — approvals filter
    excludes drafts (DocketApprovalsPage.tsx:104-107); no "started, not
    submitted" surface either side.

## Batch 3 — needs owner judgment first

- **Offline dockets**: docket creation gated canWrite = canEdit && isOnline
  (DocketScreen.tsx:501); syncWorker dead-letters docket ops (:540-545).
  Build offline queueing (recommended — adhesion loop) or make the block
  an explicit product decision.
- **"Submit claim" honesty**: downloads a 5-row CSV, flips status, sends
  nothing (ClaimsPage.tsx:243-249; submissionOptions.ts:9-15). Attach the
  real evidence package + truthful confirmation copy.
- **Create-Lot modal diet** (~10 fields → 3 + Advanced; suggested-ITP card
  vs dropdown fight, CreateLotModal.tsx:336-375; suggestion hardcoded
  'earthworks' :139-140) and **NCR form unification** (CreateNCRModal vs
  tests/NcrModals.tsx divergent copies; from-test path lands Unassigned).
- **Subbie onboarding dead-end**: setup hero routes non-admin subbie to
  read-only roster (HomeScreenHero.tsx:78-81; CompanyScreen.tsx:252) —
  "ask your admin" path instead.
- **Hold point release in-shell**: ItpRunScreen.tsx:327-329 ejects to
  desktop /hold-points.
- **Diary wizard order**: Work locked behind Weather+Crew
  (diaryStepState.ts:73-74) — allow any order, keep submit gate.
- **Project switcher stale record id** → 404 in other project
  (Header.tsx:161-165) — preserve module segment only.
- **Unsaved-changes guard** (none anywhere: no
  beforeunload/useBlocker/isDirty) + **diary autosave** (localStorage
  draft keyed project:date).
- **Claims vocabulary**: "Record Payment Schedule" button → "Record
  certification received" modal → "Certified" badge — pick one term.
- **Disputed-claim notes never shown** after entry (persisted
  claims/types.ts:28-29; ClaimsTable.tsx:251-254 renders badge only) —
  DisputeReadBack sibling to CertificationReadBack.
- **ITP un-assign** (no DELETE /instances route; wrong assignment
  permanent, instances.ts:157) + **edit-locked template warning up front**
  (EditTemplateModal only 409s on save, templateUsage.ts:92-117).
- **Two readiness surfaces disagree** on lot page (LotReadinessPanel vs
  QualityManagementSection prerequisites from separate endpoints,
  LotDetailPage.tsx:441 vs :164-171) — drive both from one payload.
- **Claim % smart default** from ITP/quality completion (CreateClaimModal
  .tsx:47-65) + **"Select all claimable"** (:296-405) + **budget-less lot
  claims $0 silently** (utils.ts:211) + **committed-pending $ on budget
  bars** (costsPageHelpers.ts:96-108) + **cost vs claimed joined view**.
- **Office "Create Docket" vestigial** (subbie-role-only button, no entry
  editor, CreateDocketModal) — remove or complete.
- **GlobalSearch**: only lots/NCRs/tests, project-scoped only, client-
  filters a limit=10 fetch (GlobalSearch.tsx:18,84,399).
- **Dashboard summary tiles** navigate with params ProjectsPage never
  reads (DashboardIssueSummaryWidgets.tsx:69,89, DashboardKpiTiles.tsx:51)
  — deep-link to scoped lists or drop the promise.
- **Doc upload / claim-submitted notify nobody**; **subbie NCR list
  unreachable** when lots module on (HomeScreen.tsx:344-353; /p/ncrs
  exists unlinked); **SubbieItpRunScreen photo-guard gap** (same as #1345,
  flagged in its body); **no pull-to-refresh in shells**; **subbie ITP run
  back-target orphan** (/p/itps instead of lot hub,
  SubbieItpRunScreen.tsx:133,291); **toast position/duration for
  important confirmations** (bottom-right 4s under thumb zone,
  toaster.tsx:8,22-27).

## Verified healthy (don't re-audit)
Lot suggest-number; bulk-create-by-chainage + clone; NCR status labels
(formatStatusLabel); claim create defaults (period, remaining-%, cap);
HP recipient pre-fill; docket reject/query feedback loop (notifications +
banners); approved dockets land in Costs; onboarding funnel + example
project; breadcrumbs; deep-link modal/back sync; double-submit guards;
required-field marking; notifications bell (works — link + unread badge);
notifications page CRUD.
