# Report surfaces overhaul — build plan (2026-07-09)

(Previous todo — variation register 2026-07-08 — completed and recorded in
PRs #1361–#1364 + memory.)

Spec: docs/research/report-surfaces-validated-spec-2026-07-09.md
Jay's directive: Batch A solo → merge on green CI → B/C/D in parallel subagents
(codex CLI for diligence-heavy items).

## Batch A — trust & branding (built solo, suites running)

- [x] Backend: `GET /api/projects/:id/branding` (readRoutes.ts) — company block
      with server-embedded logo data URL; DB-free response-helper test
- [x] Frontend: `fetchPdfBranding(projectId)` best-effort helper (lib/pdf/fetchBranding.ts)
- [x] types.ts: PDFCompanyBranding/PDFBrandingData + abn/address
- [x] branding.ts: resolvePdfBranding carries abn/address;
      formatCompanyDetailsLine/drawCompanyDetailsLine; header band renders
      details; drawPdfFooters right-aligned "Generated by CIVOS · civos.com.au"
- [x] De-CIVOS headers: dashboard subtitle → company name; conformance "- CIVOS"
      suffixes + generated-by line removed; HP generated-by line removed
- [x] Filenames: Dashboard-Summary-{date}, Daily-Diary-{date},
      Docket-{n}-{date}
- [x] Wired company into diary/docket/test-cert callers (fetchPdfBranding) and
      dashboard (GET /api/company — company-scoped)
- [x] Conformance hook prefers branding endpoint; NCRTable fetches branding
      (NCR was a 5th unbranded surface — data never carried company);
      claim + HP evidence backend payloads now embed logo + abn/address
- [x] HPPackageOptions toggles honored, sections renumber without gaps
      (+ holdPointEvidencePdf.options.test.ts, 4 tests)
- [x] Scheduled reports: company name (ABN) header line + ASCII attribution line
- [x] Characterization pins updated (filenames, CIVOS strings → footer attribution)
- [ ] Full suites green (frontend/backend running) → fallow audit → PR → CI → merge

Deferred to Batch B: ABN/address line on the claim-package COVER (cover gets
restructured in B; details would collide with the current centered title).

Skipped (ponytail): new shared style module — branding.ts already is it.
Server/data timestamps: audited; new Date() only feeds the footer "Generated"
stamp, which genuinely is generation time. No body dates use client clock. No-op.

## Batch B — money docs (parallel after A)
Claim package declaration/% wiring/itemised evidence; Xero DueDate/TaxType/ISO
dates; docket itemisation + ABN; variation CSV; claims CSV. → codex (diligence)

## Batch C — quality docs (parallel after A)
Conformance result-vs-limits + verifier; test cert → Material Conformance
Record (migration — flag to Jay); NCR concession block; HP releaser prominence;
public-link hardening check. → codex (diligence)

## Batch D — site records/registers (parallel after A)
Diary contemporaneity block + full content; delay CSV lot; register CSVs;
reports hub computed fields. → opus subagent

## Review notes
(fill as batches land)
